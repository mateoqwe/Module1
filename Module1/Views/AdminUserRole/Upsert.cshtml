@model Module1Model.Models.AdminUserRole
@{
    var title = "Create Admin User Role";
}
<form method="post">
    <div class="row p-3 border">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="adminUserRoleId" />
        @if (Model.adminUserRoleId != 0)
        {
            //edit
            title = "Edit Admin User Role";
        }
        <div class="col-12 border-bottom">
            <h2 class="text-primary">@title</h2>
        </div>
        <div class="col-8 pt-4">
            <div class="form-group row">
                <div class="col-4">
                    <label asp-for="roleName"></label>
                </div>
                <div class="col-8">
                    <input asp-for="roleName" class="form-control" />
                    <span asp-validation-for="roleName" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-4">
                    <label asp-for="rolePower"></label>
                </div>
                <div class="col-8">
                    <input asp-for="rolePower" class="form-control" />
                    <span asp-validation-for="rolePower" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-4 offset-4">
                    @if (Model.adminUserRoleId != 0)
                    {
                        <button type="submit" class="btn btn-primary form-control">Update</button>
                    }
                    else
                    {
                        <button type="submit" class="btn btn-primary form-control">Create</button>
                    }
                </div>
                <div class="col-4">
                    <a asp-action="Index" class="btn btn-success form-control">Back to List</a>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts{
    <partial name="_ValidationScriptsPartial" />
}